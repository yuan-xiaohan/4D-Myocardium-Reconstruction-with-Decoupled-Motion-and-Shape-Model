# Inference inputs

Create one `CASE_ID/masks/` directory per case. Mask frames must use contiguous
numeric NIfTI names such as `00.nii.gz`, `01.nii.gz`, and so on. The generated
case data stays ignored by Git; see the repository README for labels and config.
